Professional workflow

For contributing:

git clone
→ create branch
→ commit
→ push
→ pull request

NOT:

git init

git init is for:

brand new projects
repositories starting from scratch

not existing GitHub projects.
